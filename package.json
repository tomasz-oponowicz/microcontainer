{
  "name": "microcontainer",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "rm -Rf dist/ && mkdir dist && cp src/index.html dist/ && browserify src/app.js -o dist/app.js -t"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^15.3.1",
    "react-dom": "^15.3.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
